# Production build constraints (C-3 — reproducible builds).
#
# pyproject.toml uses floor pins (>=) for developer ergonomics. A floor pin lets a fresh
# `pip install` pull a newer minor/patch that can break the app — this actually happened:
# a co-installed tool upgraded starlette to 1.3.1, incompatible with FastAPI 0.115, and the
# app started raising `Router.__init__() got an unexpected keyword argument 'on_startup'`
# on boot. Passing this file with `pip install -c constraints.txt ...` pins the exact
# versions proven working, WITHOUT changing which packages are installed (constraints only
# cap versions of things already being resolved). Regenerate deliberately, in a clean env,
# and re-run the test suite before bumping.
#
# The starlette pin below is the single most important line — do not loosen it without
# confirming FastAPI/starlette compatibility.

fastapi==0.115.14
starlette==0.46.2
uvicorn==0.51.0
sqlalchemy==2.0.51
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.2
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.32
httpx==0.28.1
email-validator==2.3.0
dnspython==2.8.0
aiosqlite==0.22.1
alembic==1.18.5
redis==8.0.1

# Critical transitives (pinned because a surprise bump here is what broke the app):
anyio==4.14.2
click==8.4.2
h11==0.16.0
greenlet==3.5.3
typing-extensions==4.16.0
annotated-types==0.7.0
cryptography==50.0.0   # 49.0.0 carries CVE-2026-69247 (HIGH, fixed in 50.0.0)
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.4
idna==3.18
certifi==2026.6.17
Mako==1.3.12
